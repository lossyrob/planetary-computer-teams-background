# Copilot instructions

When a user asks to regenerate the current Teams background image or explain where the current image is from, use the `planetary-background-ops` repository skill.

Ground geography answers in the rendered crop geometry from the current `*-info.json`, not just the AOI or the full Sentinel item footprint.
